#ifndef ADD_MULTI_H
#define ADD_MULTI_H
#include <Scheme.hpp>

namespace evaluate {
    template <typename T>
    remove_dimension_single(SchemePtr scheme, const remove_dimension_para& , T max, T min);
}

#endif // ADD_MULTI_H
